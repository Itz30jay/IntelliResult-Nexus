package com.intelliresult.nexus.entity.enums;

/** Mirrors exams.chk_exams_type in schema.sql exactly (Sec. 9). */
public enum ExamType {
    UNIT_TEST,
    MID_SEMESTER,
    PRACTICAL,
    INTERNAL_ASSESSMENT,
    FINAL_EXAMINATION,
    IMPROVEMENT_EXAMINATION
}
